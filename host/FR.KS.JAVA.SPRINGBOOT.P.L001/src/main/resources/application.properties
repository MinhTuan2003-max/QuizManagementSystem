# APP CONFIGURATION
spring.application.name=FR_KS_JAVA_SPRINGBOOT_P_L001

# DATASOURCE (POSTGRESQL)
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:quiz_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:tuanhm43}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# LOGGING
logging.level.fpt.tuanhm43.fr_ks_java_springboot_p_l001=DEBUG
logging.level.org.springframework.security=INFO

# SECURITY & JWT CONFIGURATION
security.jwt.secret-key=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
security.jwt.expiration=${JWT_EXPIRATION:86400000}
security.jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# Actuator Monitoring
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Admin User Default
app.admin.default-password=${ADMIN_DEFAULT_PASSWORD:Admin@123456}
app.user.default-password=${USER_DEFAULT_PASSWORD:User@123456}